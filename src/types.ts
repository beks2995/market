export interface Device {
  name: string;
  price: any;
  priceWithDiscount: any;
  images: string;
  description: string;
  stock: number;
}

export interface Item {
  id: string;
  name: string;
  price: number;
 
}
